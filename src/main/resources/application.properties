spring.datasource.url=jdbc:postgres://abhimanyu:abhimanyu@localhost:5432/test
spring.datasource.username=abhimanyu
spring.datasource.password=abhimanyu
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# ===============================
# = RAZORPAY CREDS
# ===============================
RAZORPAY_BASE_URL =${RAZORPAY_BASE_URL}
RAZORPAY_API_KEY_ID = ${RAZORPAY_API_KEY_ID} 
RAZORPAY_API_KEY_SECRET = ${RAZORPAY_API_KEY_SECRET}
